
O caminho de saída main/generated-sources foi configurado **intencionalmente** para que os arquivos gerados pelo **ANTLR4** também sejam **versionados no repositório git**, facilitando a **visualização**, o **estudo** e o **compartilhamento com o professor**, porém a aplicação utiliza realmente os arquivos gerados dentro da pasta target/generated-sources.

🔗 Para mais detalhes sobre:
- a estrutura e sintaxe da linguagem criada,
- exemplos de teste,
- e a forma de execução da aplicação,

consulte o **[README.md principal](../../README.md)** localizado na raiz do projeto.
